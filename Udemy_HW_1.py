# 1. Create a list with 5 elements of different types
first_list = ['Python', True, 555, 10.5, {'sum': 100}]

# 2. Delete the third element
first_list.pop(2)

# 3. Output the length of the list to the terminal
print(len(first_list))

# 4. Change the order of elements in the list
first_list.reverse()
print(first_list)

# 5. Create another list with two items
second_list = ['Java', 'PHP']

# 6. Expand the first list by elements of the second list
first_list.extend(second_list)

# 7. Output an extended list of 6 items to the discrimination
print(first_list)